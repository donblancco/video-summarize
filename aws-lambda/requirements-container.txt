# Container Lambda用の軽量依存関係
# FFmpegバイナリを直接使用するため、moviepy/pydubは不要

# OpenAI API for transcription and article generation (Lambda安定版)
openai==0.28.0

# AWS SDK (Lambda環境では標準で利用可能だが、バージョン固定のため明示)
boto3==1.34.144

# WordPress投稿用
requests==2.31.0
beautifulsoup4==4.12.3

# その他ユーティリティ
python-dateutil==2.9.0